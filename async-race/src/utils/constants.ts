import { Path } from '../types/type';

export const URL = 'http://127.0.0.1:3000';
export const PATH_MAP: Record<string, Path> = {
  garage: '/garage',
  engine: '/engine',
  winners: '/winners',
};

export const START_PAGE_GARAGE = 1;
export const CARS_ON_PAGE = 7;
